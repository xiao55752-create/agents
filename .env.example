# agentOS v0.2 — 复制为 .env 并填入密钥（可选，不填则走演示模拟模式）

# API
PORT=8080
HOST=127.0.0.1
MAX_CONCURRENT_RUNS=2

# Anthropic — 配置后 Agent 会调用真实 LLM 生成修复方案
ANTHROPIC_API_KEY=

# GitHub — Personal Access Token（repo 权限），配置后会在 Issue 上回写评论
GITHUB_TOKEN=

# 数据文件路径（默认 ./data/store.json）
# DATABASE_PATH=
